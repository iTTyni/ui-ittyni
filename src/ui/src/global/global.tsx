import { createGlobalStyle } from "../../../theme/styled-components";

export const GlobalStyle = createGlobalStyle`
  *, body, root { 
    margin: 0; 
    padding: 0; 
  }

  h2,h3 {
    font-size : .9em;
  }
`