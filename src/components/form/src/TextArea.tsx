import { StyledTextArea } from "../../../../";

/**
 * Select Component for iTTyni.com
 * @param 
 * @returns 
 */
export const TextArea: React.FC<any> = (props) => (
    <StyledTextArea
      {...props}
    />
  )